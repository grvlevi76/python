#<======================== tuple ==========================>
#tuple is same as list except tuples r immutable
#elements taken as input through eval(input()) makes sequence a tuple
#can be defined with parenthesis () or without it , with elements seperated by comma
#tuple = (1,) -> seperate element with comma even if its one element else it will count as integer
#many of list functioons works on tuple too